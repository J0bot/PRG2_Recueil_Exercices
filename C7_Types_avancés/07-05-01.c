#include <stdio.h>
#include <string.h>

/**
 * Exercice: Utilisation des constantes
 * 
 * Instructions:
 * Écrire une fonction C qui détecte la présence d'un motif (chaîne de caractères)
 * à l'intérieur d'une chaîne de caractères. Cette fonction pourra elle-même utiliser
 * la fonction strstr().
 * 
 * La fonction principale devra appeler cette fonction pour détecter la présence
 * de la chaine "subcmd" dans le tableau de chaînes suivant:
 *    Chaîne 1: "commande1 subcmd"
 *    Chaîne 2: "autre commande"
 *    Chaîne 3: "encore subcmd ici"
 *    Chaîne 4: "dernière chaîne"
 * 
 * Ces chaînes seront déclarées dans un tableau de chaînes de caractères.
 * Vous devez utiliser des constantes autant que faire se peut.
 */

// Fonction pour vérifier si une chaîne contient un motif
// Utilisez le qualificateur const pour les paramètres
int contientMotif(/* Votre code ici */) {
    // Votre code ici
    return 0;
}

int main() {
    // Tableau de chaînes de caractères
    // N'oubliez pas d'utiliser const
    // Votre code ici
    
    // Parcours du tableau pour détecter le motif
    // Votre code ici
    
    return 0;
}
