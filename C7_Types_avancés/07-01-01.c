#include <stdio.h>
#include <string.h>

/**
 * Exercice: Utilisation des structures - Inventaire de livres
 * 
 * Instructions:
 * 1. Créer une struct appelée Livre qui stocke le titre, l'auteur, 
 *    l'année de publication, et le nombre de copies.
 * 
 * 2. Implémenter une fonction pour ajouter un nouveau livre à l'inventaire 
 *    et une fonction pour mettre à jour le stock lorsqu'un livre est emprunté 
 *    ou retourné.
 */

// Définition de la structure Livre
typedef struct {
    // Votre code ici
} Livre;

// Fonction pour ajouter un nouveau livre
Livre ajouterLivre(char *titre, char *auteur, int annee, int nbCopies) {
    // Votre code ici
    Livre l;
    return l;
}

// Fonction pour mettre à jour le stock
void mettreAJourStock(Livre *l, int changement) {
    // Votre code ici
}

// Fonction pour afficher les informations d'un livre
void imprimerLivre(Livre l) {
    // Votre code ici
}

int main() {
    // Créer un livre et tester les fonctions
    // Votre code ici
    
    return 0;
}
